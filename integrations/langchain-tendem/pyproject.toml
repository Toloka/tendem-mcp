[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "langchain-tendem"
version = "0.2.0"
description = "Human-in-the-loop for LangChain agents — price-capped tools that delegate work to vetted human experts via Tendem, polling in plain Python (no LLM, no tokens) until the verified result returns."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Toloka AI BV", email = "support@tendem.ai" }]
keywords = [
  "langchain",
  "langgraph",
  "mcp",
  "model-context-protocol",
  "tendem",
  "human-in-the-loop",
  "agents",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: Implementation :: CPython",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Typing :: Typed",
]
dependencies = [
  "httpx>=0.27,<1.0",
  "langchain-core>=1.0,<2.0",
  "langchain-mcp-adapters>=0.3.2,<0.4",
  "mcp>=1.24,<2.0",
  "pydantic>=2.7,<3.0",
  "typing-extensions>=4.14",
]

[project.optional-dependencies]
test = ["pytest>=8.0", "pytest-asyncio>=1.0"]

[project.urls]
Homepage = "https://tendem.ai"
Documentation = "https://github.com/Toloka/tendem-mcp/tree/main/integrations/langchain-tendem#readme"
Repository = "https://github.com/Toloka/tendem-mcp"
Issues = "https://github.com/Toloka/tendem-mcp/issues"
Changelog = "https://github.com/Toloka/tendem-mcp/releases"

[tool.hatch.build.targets.wheel]
packages = ["src/langchain_tendem"]

[tool.hatch.build.targets.sdist]
include = ["src/langchain_tendem", "tests", "README.md", "LICENSE"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "D"]
ignore = ["D107", "D203", "D205", "D213", "D400", "D401", "D415"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["D"]
